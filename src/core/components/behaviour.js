import Component from './component';

class Behaviour extends Component {
    constructor(...props) {
        super(...props);
    }

    start() {
    }

    update() {
    }

    onCollision(/*other*/) {
    }
}

export default Behaviour;
